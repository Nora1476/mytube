extends base
include mixins/video

block content  

  div.video__grid
    each info in videos
      +video(info)
      +video(info)
      +video(info)
      +video(info)
    else 
      span.empty__message No Videios found.